name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  linux:
    name: Linux Clang quality
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      CC: clang-22
      CXX: clang++-22
      CCACHE_COMPILERCHECK: content
      CCACHE_MAXSIZE: 2G

    steps:
      - name: Check out source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Install LLVM 22 quality tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes ca-certificates cmake curl gnupg ninja-build \
            libfontconfig1-dev libva-dev python3-dev ccache
          llvm_key=$(mktemp)
          trap 'rm -f "$llvm_key"' EXIT
          curl --fail --silent --show-error --location \
            https://apt.llvm.org/llvm-snapshot.gpg.key \
            --output "$llvm_key"
          primary_fingerprint=$(gpg --show-keys --with-colons "$llvm_key" | awk -F: '$1 == "fpr" {print $10; exit}')
          test "$primary_fingerprint" = 6084F3CF814B57C1CF12EFD515CF4D18AF4F7421
          gpg --dearmor < "$llvm_key" | \
            sudo tee /usr/share/keyrings/llvm-snapshot.gpg >/dev/null
          printf '%s\n' \
            'deb [signed-by=/usr/share/keyrings/llvm-snapshot.gpg] https://apt.llvm.org/noble/ llvm-toolchain-noble-22 main' | \
            sudo tee /etc/apt/sources.list.d/llvm-22.list >/dev/null
          sudo apt-get update
          sudo apt-get install --yes clang-22 clang-format-22 clang-tidy-22

      # The key suffix is sha256(sorted "<component> <archiveSha256>\n" lines for every
      # dependencies.lock.json component that has its own downloaded archive), so it changes
      # deterministically whenever the locked archive set changes (a component added/removed/
      # re-versioned) and is unaffected by lock member reordering. openjph is a lock component but
      # is vendored inside the OpenEXR archive (see dependencies/licenses/openjph/provenance.md)
      # rather than separately downloaded, so it contributes no line of its own; its bytes are
      # already covered by the openexr line. The key started as the yyjson archive digest alone
      # (the first locked component); imath, libdeflate, and openexr's intake extended it to six;
      # opencolorio's intake (and its expat/yaml-cpp/pystring/minizip-ng transitives), plus the
      # OpenH264 source archive, extends it to the current fourteen-line component set per this
      # convention rather than embedding every raw digest in the key.
      - name: Cache dependency archives
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ${{ runner.temp }}/bloom-dependency-downloads
          key: bloom-dependency-archives-82571923722fa396b5d40449e487c86b8ab421e66581f34a0de828b9e13eb9e8

      - name: Cache dependency build
        id: cache-dependency-prefix
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          # The qualified prefix's OpenH264 link stub names the private OpenH264 install by
          # absolute path, so the superbuild tree that holds it must restore alongside the prefix.
          path: |
            ${{ runner.temp }}/bloom-superbuild
            build/dependency-prefix
          key: bloom-dependency-build-${{ runner.os }}-${{ hashFiles('dependencies/dependencies.lock.json') }}

      - name: Cache ccache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ~/.cache/ccache
          key: bloom-ccache-linux-native-${{ hashFiles('dependencies/dependencies.lock.json') }}
          restore-keys: |
            bloom-ccache-linux-native-

      - name: Build dependency superbuild prefix
        if: steps.cache-dependency-prefix.outputs.cache-hit != 'true'
        run: |
          cmake -S dependencies/superbuild -B "${{ runner.temp }}/bloom-superbuild" -G Ninja \
            -DBLOOM_DEPENDENCY_DOWNLOAD_DIR="${{ runner.temp }}/bloom-dependency-downloads" \
            -DBLOOM_DEPENDENCY_PREFIX="$GITHUB_WORKSPACE/build/dependency-prefix" \
            -DBLOOM_FFMPEG_ENABLE_VAAPI=ON
          cmake --build "${{ runner.temp }}/bloom-superbuild"

      - name: Configure Clang and clang-tidy
        run: |
          tidy=()
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            tidy=(-DBLOOM_ENABLE_CLANG_TIDY=OFF -DBLOOM_ENABLE_TEST_CLANG_TIDY=OFF)
          fi
          cmake --preset ci-linux-native -DBLOOM_BUILD_PYTHON=ON "${tidy[@]}" \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache

      - name: Check repository quality
        run: cmake --build --preset ci-linux-native --target bloom-quality-check --parallel

      - name: Build
        run: cmake --build --preset ci-linux-native --parallel

      # Pull requests analyze only the translation units they change; the full sweep runs when the
      # change lands on main. clang-tidy during compilation is disabled above for pull requests.
      - name: Tidy changed translation units
        if: github.event_name == 'pull_request'
        run: |
          git fetch --quiet --depth=1 origin "${{ github.event.pull_request.base.sha }}"
          checks=$(grep -m1 '^BLOOM_CLANG_TIDY_CHECKS:' build/ci-linux-native/CMakeCache.txt | cut -d= -f2-)
          tools/quality/tidy_changed_files \
            --compile-database build/ci-linux-native/compile_commands.json \
            --checks "$checks" \
            --header-filter "^$GITHUB_WORKSPACE/(apps|src|tests|tools)/" \
            --base "${{ github.event.pull_request.base.sha }}" \
            --clang-tidy clang-tidy-22 \
            --jobs "$(nproc)"

      - name: Test Clang build
        run: ctest --preset ci-linux-native --output-on-failure

      - name: Consume prefix in qualified mode
        run: |
          cmake -S tests/dependency-consumption -B "${{ runner.temp }}/bloom-consume" -G Ninja \
            -DBLOOM_DEPENDENCY_MODE=qualified \
            -DBLOOM_DEPENDENCY_PREFIX="$GITHUB_WORKSPACE/build/dependency-prefix" \
            -DBLOOM_MEDIA_WORKER_RUNTIME="$GITHUB_WORKSPACE/build/ci-linux-native/apps/bloom-media-worker/lib"
          cmake --build "${{ runner.temp }}/bloom-consume"
          "${{ runner.temp }}/bloom-consume/bloom_consume_smoke"

  linux-desktop:
    name: Linux Qt desktop
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    env:
      CC: clang-22
      CXX: clang++-22
      # Widgets tests run without a display server; the offscreen platform keeps them headless.
      QT_QPA_PLATFORM: offscreen
      # clang-tidy still runs per translation unit, but ccache keeps the (large) Qt shell compile
      # from repeating across runs. The content check is stable across cache restores.
      CCACHE_COMPILERCHECK: content
      CCACHE_MAXSIZE: 2G

    steps:
      - name: Check out source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Install LLVM 22 quality tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes ca-certificates cmake curl gnupg ninja-build \
            libgl-dev libegl-dev libfontconfig1-dev libxkbcommon-dev libva-dev python3-dev ccache
          llvm_key=$(mktemp)
          trap 'rm -f "$llvm_key"' EXIT
          curl --fail --silent --show-error --location \
            https://apt.llvm.org/llvm-snapshot.gpg.key \
            --output "$llvm_key"
          primary_fingerprint=$(gpg --show-keys --with-colons "$llvm_key" | awk -F: '$1 == "fpr" {print $10; exit}')
          test "$primary_fingerprint" = 6084F3CF814B57C1CF12EFD515CF4D18AF4F7421
          gpg --dearmor < "$llvm_key" | \
            sudo tee /usr/share/keyrings/llvm-snapshot.gpg >/dev/null
          printf '%s\n' \
            'deb [signed-by=/usr/share/keyrings/llvm-snapshot.gpg] https://apt.llvm.org/noble/ llvm-toolchain-noble-22 main' | \
            sudo tee /etc/apt/sources.list.d/llvm-22.list >/dev/null
          sudo apt-get update
          sudo apt-get install --yes clang-22 clang-format-22 clang-tidy-22

      - name: Cache Qt
        id: cache-qt
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ${{ runner.temp }}/qt
          key: bloom-qt-6.8.3-linux-gcc-64

      - name: Install Qt 6.8
        if: steps.cache-qt.outputs.cache-hit != 'true'
        # Runs from the runner temp directory: aqt writes aqtinstall.log into its working
        # directory, and a stray log in the workspace fails the repository hygiene check.
        working-directory: ${{ runner.temp }}
        run: |
          python3 -m pip install --user aqtinstall==3.2.1
          python3 -m aqt install-qt linux desktop 6.8.3 linux_gcc_64 \
            --outputdir "${{ runner.temp }}/qt"

      - name: Cache ccache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ~/.cache/ccache
          key: bloom-ccache-linux-desktop-${{ hashFiles('dependencies/dependencies.lock.json') }}
          restore-keys: |
            bloom-ccache-linux-desktop-

      # The key suffix is sha256(sorted "<component> <archiveSha256>\n" lines for every
      # dependencies.lock.json component that has its own downloaded archive), so it changes
      # deterministically whenever the locked archive set changes (a component added/removed/
      # re-versioned) and is unaffected by lock member reordering. openjph is a lock component but
      # is vendored inside the OpenEXR archive (see dependencies/licenses/openjph/provenance.md)
      # rather than separately downloaded, so it contributes no line of its own; its bytes are
      # already covered by the openexr line. The key started as the yyjson archive digest alone
      # (the first locked component); imath, libdeflate, and openexr's intake extended it to six;
      # opencolorio's intake (and its expat/yaml-cpp/pystring/minizip-ng transitives), plus the
      # OpenH264 source archive, extends it to the current fourteen-line component set per this
      # convention rather than embedding every raw digest in the key.
      - name: Cache dependency archives
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ${{ runner.temp }}/bloom-dependency-downloads
          key: bloom-dependency-archives-82571923722fa396b5d40449e487c86b8ab421e66581f34a0de828b9e13eb9e8

      - name: Cache dependency build
        id: cache-dependency-prefix
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          # The qualified prefix's OpenH264 link stub names the private OpenH264 install by
          # absolute path, so the superbuild tree that holds it must restore alongside the prefix.
          path: |
            ${{ runner.temp }}/bloom-superbuild
            build/dependency-prefix
          key: bloom-dependency-build-${{ runner.os }}-${{ hashFiles('dependencies/dependencies.lock.json') }}

      - name: Build dependency superbuild prefix
        if: steps.cache-dependency-prefix.outputs.cache-hit != 'true'
        run: |
          cmake -S dependencies/superbuild -B "${{ runner.temp }}/bloom-superbuild" -G Ninja \
            -DBLOOM_DEPENDENCY_DOWNLOAD_DIR="${{ runner.temp }}/bloom-dependency-downloads" \
            -DBLOOM_DEPENDENCY_PREFIX="$GITHUB_WORKSPACE/build/dependency-prefix" \
            -DBLOOM_FFMPEG_ENABLE_VAAPI=ON
          cmake --build "${{ runner.temp }}/bloom-superbuild"

      - name: Configure desktop build
        run: |
          tidy=()
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            tidy=(-DBLOOM_ENABLE_CLANG_TIDY=OFF)
          fi
          cmake --preset ci-linux-desktop -DBLOOM_BUILD_PYTHON=ON "${tidy[@]}" \
            -DCMAKE_PREFIX_PATH="${{ runner.temp }}/qt/6.8.3/gcc_64" \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache

      - name: Build desktop
        run: cmake --build --preset ci-linux-desktop --parallel

      # Covers the desktop-only units the native job's database does not contain.
      - name: Tidy changed translation units
        if: github.event_name == 'pull_request'
        run: |
          git fetch --quiet --depth=1 origin "${{ github.event.pull_request.base.sha }}"
          checks=$(grep -m1 '^BLOOM_CLANG_TIDY_CHECKS:' build/ci-linux-desktop/CMakeCache.txt | cut -d= -f2-)
          tools/quality/tidy_changed_files \
            --compile-database build/ci-linux-desktop/compile_commands.json \
            --checks "$checks" \
            --header-filter "^$GITHUB_WORKSPACE/(apps|src|tests|tools)/" \
            --base "${{ github.event.pull_request.base.sha }}" \
            --clang-tidy clang-tidy-22 \
            --jobs "$(nproc)"

      - name: Test desktop build
        run: ctest --preset ci-linux-desktop --output-on-failure

      - name: Upload golden captures on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: golden-captures-${{ github.run_id }}
          path: build/*.png
          if-no-files-found: ignore
          retention-days: 7

  # macOS native/core qualification. The desktop and media provider are Apple-framework
  # implementations (CoreText font catalogue, POSIX staged publication and process supervision, and
  # the VideoToolbox/AVFoundation media worker), so this job builds and tests them off the Qt shell.
  # Non-required: Linux remains the automated merge gate.
  macos-native:
    name: macOS Clang quality
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      # `src/core` uses floating-point std::from_chars/std::to_chars, which Apple's libc++ only
      # ships from a recent Xcode; select the newest installed toolchain.
      - name: Select newest Xcode
        run: |
          newest=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1 || true)
          if [ -n "$newest" ]; then sudo xcode-select -s "$newest"; fi
          xcodebuild -version || clang++ --version | head -1

      - name: Install dependencies
        run: brew install opencolorio openexr libzip yyjson zlib ninja

      - name: Configure
        run: |
          cmake -S . -B build/macos-native -G Ninja \
            -DBLOOM_DEPENDENCY_MODE=developer-system \
            -DBLOOM_BUILD_DESKTOP=OFF \
            -DBLOOM_BUILD_PYTHON=OFF \
            -DBUILD_TESTING=ON \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH=/opt/homebrew

      - name: Build
        run: cmake --build build/macos-native --parallel

      - name: Test
        run: ctest --test-dir build/macos-native --output-on-failure -j 3 --timeout 900

  # macOS desktop build: the Qt shell, the bundled media worker, and macdeployqt packaging.
  # Non-required for the same reason as macos-native.
  macos-desktop:
    name: macOS Qt desktop
    runs-on: macos-latest
    timeout-minutes: 90

    steps:
      - name: Check out source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Select newest Xcode
        run: |
          newest=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1 || true)
          if [ -n "$newest" ]; then sudo xcode-select -s "$newest"; fi
          xcodebuild -version || clang++ --version | head -1

      - name: Install dependencies
        run: brew install opencolorio openexr libzip yyjson zlib ninja

      - name: Install Qt
        # aqt writes aqtinstall.log into its working directory; run it outside the checkout so the
        # repository hygiene check never sees it.
        working-directory: ${{ runner.temp }}
        run: |
          python3 -m venv "$RUNNER_TEMP/aqt-venv"
          "$RUNNER_TEMP/aqt-venv/bin/pip" install --quiet aqtinstall==3.2.1
          "$RUNNER_TEMP/aqt-venv/bin/aqt" install-qt mac desktop 6.8.3 clang_64 \
            --outputdir "$RUNNER_TEMP/qt"

      # Some macOS SDKs removed the deprecated AGL framework that Qt's link line still names. Provide
      # an empty stub so linking succeeds; it contributes no symbols.
      - name: Provide AGL framework stub
        run: |
          mkdir -p "$RUNNER_TEMP/agl-frameworks/AGL.framework/Resources"
          printf 'void __bloom_agl_stub(void) {}\n' > "$RUNNER_TEMP/agl.c"
          clang -c "$RUNNER_TEMP/agl.c" -o "$RUNNER_TEMP/agl.o"
          ar rcs "$RUNNER_TEMP/agl-frameworks/AGL.framework/AGL" "$RUNNER_TEMP/agl.o"

      - name: Configure
        run: |
          cmake -S . -B build/macos-desktop -G Ninja \
            -DBLOOM_DEPENDENCY_MODE=developer-system \
            -DBLOOM_BUILD_DESKTOP=ON \
            -DBLOOM_BUILD_PYTHON=OFF \
            -DBUILD_TESTING=ON \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$RUNNER_TEMP/qt/6.8.3/macos;/opt/homebrew" \
            -DCMAKE_EXE_LINKER_FLAGS="-F$RUNNER_TEMP/agl-frameworks" \
            -DCMAKE_SHARED_LINKER_FLAGS="-F$RUNNER_TEMP/agl-frameworks"

      - name: Build
        run: cmake --build build/macos-desktop --parallel

      - name: Test
        env:
          QT_QPA_PLATFORM: offscreen
        # bloom.ui.background-preview has a load-sensitive asynchronous fill assertion that fails
        # intermittently only on the shared macOS runner (not reproducible on this project's own
        # hardware at any load). Retry once so a timing flake does not mask the rest of the suite;
        # a genuine regression still fails both attempts.
        run: ctest --test-dir build/macos-desktop --output-on-failure -j 3 --timeout 900 --repeat until-pass:2

      - name: Package Bloom.app with macdeployqt
        run: cmake --build build/macos-desktop --target bloom-deploy-macos

